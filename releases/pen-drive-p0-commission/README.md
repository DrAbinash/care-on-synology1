# Pen-drive Super Admin — P0-3 commission fix (2026-08-30)

Download **`pen-drive-p0-commission-2026-08-30.zip`** from this folder on the PR branch, then:

1. Unzip
2. Copy `superadmin-api.js` and `superadmin-ui.js` to the **top level** of your Super Admin USB drive
3. Keep your existing `superadmin.key` / `superadmin.pin`
4. Deploy/merge care-api PR #650 first, then re-pair the drive (ERP Ctrl+Shift+K)

See `INSTALL.txt` inside the zip for details.
