# Overlay for the live NAS .env (beside docker-compose.yml).
# Do not replace the whole file — merge these keys. Keep Hope callback as-is
# (live: http://192.168.1.137:7080/api/integration/care-callback).
#
# After merge: recreate care-api so Compose injects TZ / OLLAMA_NUM_CTX / OLLAMA_THINK.
# docker compose up -d --force-recreate api

ENABLE_SCHEDULERS=1
TZ=Asia/Kolkata
AI_MODEL_VISION=qwen3-vl:8b
OLLAMA_NUM_CTX=16384
OLLAMA_THINK=0
AI_CONCURRENCY=1
