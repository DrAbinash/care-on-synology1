From 92f0aa8cf2e9184c035310c2f47b79207474d616 Mon Sep 17 00:00:00 2001
From: Cursor Agent <cursoragent@cursor.com>
Date: Fri, 31 Jul 2026 19:03:52 +0000
Subject: [PATCH] Add Hope integration bootstrap and Synology deploy env

- deploy/synology/care.env with INTEGRATION_HOPE_* + HOPE_PARTNER_KEY
- bootstrap-hope-care-integration.mjs (idempotent partner + feature flag)
- docker-compose + Dockerfile pass Hope integration env vars
- deploy README with NAS setup steps
---
 .env.example                                |   8 ++
 Dockerfile                                  |   1 +
 deploy/synology/README.md                   |  59 ++++++++++
 deploy/synology/care.env                    |  88 +++++++++++++++
 docker-compose.yml                          |   6 +
 scripts/bootstrap-hope-care-integration.mjs | 117 ++++++++++++++++++++
 6 files changed, 279 insertions(+)
 create mode 100644 deploy/synology/README.md
 create mode 100644 deploy/synology/care.env
 create mode 100644 scripts/bootstrap-hope-care-integration.mjs

diff --git a/.env.example b/.env.example
index 867eb1c3..2a08db44 100644
--- a/.env.example
+++ b/.env.example
@@ -170,6 +170,14 @@ OBJECT_STORAGE_DIR=/app/data/object-storage
 # Example: https://caredeoghar.com,https://app.caredeoghar.com
 ALLOWED_ORIGINS=
 
+# ─── HOPE Hospital integration (192.168.1.137:7080) ───────────────────────────
+# Copy values from deploy/synology/care.env. Run once after deploy:
+#   docker compose exec api node scripts/bootstrap-hope-care-integration.mjs
+INTEGRATION_HOPE_CALLBACK_URL=http://192.168.1.137:7080/api/integration/care-callback
+INTEGRATION_HOPE_SIGNING_SECRET=
+HOPE_CARE_INTEGRATION_FORCE=1
+HOPE_PARTNER_KEY=
+
 # ─── CENTRAL NETWORK CONFIGURATION (Phase B — never hardcode) ────────────────
 # All network host/port fallbacks now live in exactly two files:
 #   Backend:  artifacts/api-server/src/lib/networkDefaults.ts
diff --git a/Dockerfile b/Dockerfile
index 4f50fca0..5754c452 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -119,6 +119,7 @@ ENV BUILD_DATE=${BUILD_DATE}
 COPY --from=api-build /repo/artifacts/api-server/dist             ./dist
 COPY --from=api-build /repo/artifacts/api-server/package.json     ./package.json
 COPY --from=api-build /api-deploy/node_modules                    ./node_modules
+COPY --from=api-build /repo/scripts/bootstrap-hope-care-integration.mjs ./scripts/bootstrap-hope-care-integration.mjs
 # Bake version.json into the image so /api/system/version can read it
 COPY --from=api-build /repo/version.json                          ./version.json
 
diff --git a/deploy/synology/README.md b/deploy/synology/README.md
new file mode 100644
index 00000000..738a809d
--- /dev/null
+++ b/deploy/synology/README.md
@@ -0,0 +1,59 @@
+# Synology deploy — Hope Hospital + Care Diagnostics
+
+Both systems run on **NAS `192.168.1.137`**.
+
+| System | LAN URL | Copy env to |
+|--------|---------|-------------|
+| **Hope ERP** | http://192.168.1.137:7080/ | Hope repo root → `.env` |
+| **Care ERP** | http://192.168.1.137:8888/ (public: https://caredeoghar.com) | `care-on-synology1/` → `.env` |
+
+## 1. Hope Hospital
+
+```bash
+cd /volume1/docker/hope-erp   # your Hope project folder
+cp deploy/synology/hope.env .env
+docker compose up -d --build
+```
+
+Login: `abinashsingh` / PIN from `INITIAL_ADMIN_PIN` (or boot log).
+
+## 2. Care Diagnostics
+
+```bash
+cd /volume1/docker/care-on-synology1
+cp deploy/synology/care.env .env
+docker compose up -d --build
+```
+
+## 3. Wire Hope ↔ Care (one time)
+
+After both databases are up:
+
+```bash
+cd /volume1/docker/care-on-synology1
+docker compose exec api node scripts/bootstrap-hope-care-integration.mjs
+```
+
+This enables the `ff_hope_care_referrals` flag and registers the **HOPE** partner key
+(already present in both `.env` files).
+
+## 4. Test catalogue (both systems)
+
+The CSV does **not** auto-sync. Import in **both**:
+
+- **Hope:** Clinical Masters → CARE Catalogue → Upload CSV
+- **Care:** Tests → import the same `test-catalog-*.csv`
+
+## 5. Medicines (Hope Medicals only)
+
+Pharmacy stock lives under entity **Hope Medicals** (teal sidebar). It does not
+appear in Care ERP or hospital Materials — that is correct.
+
+## 6. Smoke test
+
+1. Doctor saves OPD prescription with lab test (e.g. CBC).
+2. Care ERP → **HOPE Referrals** inbox shows the referral.
+3. After report finalisation in Care, result appears in Hope patient record.
+
+If referrals fail, try changing Hope `CARE_REFERRAL_URL` to
+`https://caredeoghar.com/api/integration/v1`.
diff --git a/deploy/synology/care.env b/deploy/synology/care.env
new file mode 100644
index 00000000..79830f26
--- /dev/null
+++ b/deploy/synology/care.env
@@ -0,0 +1,88 @@
+# =============================================================================
+# Care Diagnostics ERP — Synology NAS .env
+# Copy to care-on-synology1 folder beside docker-compose.yml:
+#   cp deploy/synology/care.env .env
+# Then: docker compose up -d --build
+# Public: https://caredeoghar.com   LAN: http://192.168.1.137:8888
+# =============================================================================
+
+HOST_PORT=8888
+DB_HOST_PORT=5400
+DB_USER=erp
+DB_PASSWORD=changeme
+DB_NAME=diagnostic_erp
+
+JWT_SECRET=fa93544ce0b0d3e816c044bd7cabd3d50f8a583b4acec08a41950f1950cc8b10
+SESSION_SECRET=8724011826d9d4ef51f242229e633b9d32a7b57d763a9804112e5d5c19a9ed0a
+BACKUP_PASSPHRASE=d0a77418b1174e3c55396ae1cf0d3440661d763d476cbecaa466962be52f9108
+
+LOG_LEVEL=info
+OBJECT_STORAGE_DIR=/app/data/object-storage
+PUBLIC_BASE_URL=https://caredeoghar.com
+
+ENABLE_SCHEDULERS=1
+CRON_SECRET=473ef5ca0140c4b30905c7f96d8bd85714917bd0542258019ae72a4f5cffcc93
+BACKUP_DEADMAN_MAX_AGE_HOURS=26
+TZ=Asia/Kolkata
+
+SUPER_ADMIN_USB_KEY=64217fdd1f59cde4326addecabbfff45d334feba2188ba3334227fcf20d67b76
+SUPER_ADMIN_USB_PIN=2321
+
+ICICI_BASE_URL=https://pgpay.icicibank.com
+ICICI_URL_PREFIX=
+ICICI_MERCHANT_ID=100000000455452
+ICICI_AGGREGATOR_ID=100000000455451
+ICICI_SECRET_KEY=d350487e-e1ec-452e-994e-bddb9fb96605
+
+FINGERPRINT_BRIDGE_SECRET=
+
+ORTHANC_URL=http://192.168.1.137:8042
+ORTHANC_USERNAME=admin
+ORTHANC_PASSWORD=
+PACS_PROVIDER=orthanc
+PACS_VIEWER_TYPE=ohif
+OHIF_URL=http://192.168.1.137:3010
+WADO_URL=http://192.168.1.137:8042/wado
+WEASIS_WADO_PUBLIC_URL=http://192.168.1.137:8042/wado
+ORTHANC_CHANGES_POLLER=false
+INTERNAL_API_KEY=1234
+ALLOW_PRIVATE_IPS=true
+
+NETWORK_LAN_HOST=192.168.1.137
+ERP_HTTP_PORT=8888
+
+OLLAMA_PRIMARY_URL=http://192.168.1.250:11434
+OLLAMA_FALLBACK_URL=http://172.16.1.140:11434
+OLLAMA_BASE_URL=http://192.168.1.250:11434
+OLLAMA_DEFAULT_MODEL=gemma3:4b
+
+OCR_ENGINE=paddle
+OCR_PROFILE=fast
+OCR_DEVICE=cpu
+OCR_LOW_CONFIDENCE_THRESHOLD=0.80
+OCR_RETRY_ACCURATE=true
+OCR_TESSERACT_FALLBACK=true
+OCR_WORKER_URL=http://192.168.1.250:8090
+OCR_WORKER_CONCURRENCY=1
+OCR_WORKER_TOKEN=WJoYapZ7mDIaojJM6umdAEUxOpNKZKodmpl2EPiD6eQx
+
+AI_MODE=AUTO
+AI_MODEL_FAST=gemma3:4b
+AI_MODEL_STANDARD=gemma3:4b
+AI_MODEL_LARGE=gemma3:12b
+AI_MODEL_VISION=gemma3:4b
+AI_TIMEOUT_FAST_SECONDS=120
+AI_TIMEOUT_LARGE_SECONDS=300
+AI_CONCURRENCY=1
+AI_TEMPERATURE_EXTRACTION=0
+AI_TEMPERATURE_DRAFT=0.1
+
+# -----------------------------------------------------------------------------
+# Hope Hospital integration (callbacks + signed results to Hope ERP)
+# Must match Hope .env CARE_CALLBACK_SECRET exactly.
+# Hope LAN URL: http://192.168.1.137:7080/
+# -----------------------------------------------------------------------------
+INTEGRATION_HOPE_CALLBACK_URL=http://192.168.1.137:7080/api/integration/care-callback
+INTEGRATION_HOPE_SIGNING_SECRET=7ab91cf3b7a45c4a3b4a6a90aa63ed2be921abc77bbd007f0de60093ba895f0f
+HOPE_CARE_INTEGRATION_FORCE=1
+HOPE_PARTNER_KEY=intgk_8ffb1b9c5b982148cfbe89448064cc4986b172bea48fe73b0f622f4a192da7e7
diff --git a/docker-compose.yml b/docker-compose.yml
index b7abfabb..14df2430 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -280,6 +280,12 @@ services:
       OBJECT_STORAGE_DIR: ${OBJECT_STORAGE_DIR:-/app/data/object-storage}
       PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-https://caredeoghar.com}
 
+      # ── Hope Hospital integration ──────────────────────────────────────────
+      INTEGRATION_HOPE_CALLBACK_URL: ${INTEGRATION_HOPE_CALLBACK_URL:-}
+      INTEGRATION_HOPE_SIGNING_SECRET: ${INTEGRATION_HOPE_SIGNING_SECRET:-}
+      HOPE_CARE_INTEGRATION_FORCE: ${HOPE_CARE_INTEGRATION_FORCE:-}
+      HOPE_PARTNER_KEY: ${HOPE_PARTNER_KEY:-}
+
       ICICI_BASE_URL: ${ICICI_BASE_URL:-https://pgpay.icicibank.com}
       ICICI_URL_PREFIX: ${ICICI_URL_PREFIX:-/pg/api/v2}
       ICICI_MERCHANT_ID: ${ICICI_MERCHANT_ID:-100000000455452}
diff --git a/scripts/bootstrap-hope-care-integration.mjs b/scripts/bootstrap-hope-care-integration.mjs
new file mode 100644
index 00000000..b3cff8f5
--- /dev/null
+++ b/scripts/bootstrap-hope-care-integration.mjs
@@ -0,0 +1,117 @@
+#!/usr/bin/env node
+/**
+ * One-time (idempotent) Hope ↔ Care integration bootstrap for Synology.
+ *
+ * - Registers HOPE partner with API key from HOPE_PARTNER_KEY env (or .env)
+ * - Enables ff_hope_care_referrals feature flag
+ *
+ * Run after deploy:
+ *   docker compose exec api node scripts/bootstrap-hope-care-integration.mjs
+ *
+ * Or locally with DATABASE_URL pointing at Care Postgres.
+ */
+import { createHash } from "node:crypto";
+import pg from "pg";
+import { readFileSync, existsSync } from "node:fs";
+import { resolve, dirname } from "node:path";
+import { fileURLToPath } from "node:url";
+
+const __dir = dirname(fileURLToPath(import.meta.url));
+const ROOT = resolve(__dir, "..");
+
+const DEFAULT_PARTNER_KEY =
+  "intgk_8ffb1b9c5b982148cfbe89448064cc4986b172bea48fe73b0f622f4a192da7e7";
+const DEFAULT_CALLBACK =
+  "http://192.168.1.137:7080/api/integration/care-callback";
+
+const HOPE_PERMISSIONS = [
+  "diagnostic_referral:create",
+  "diagnostic_referral:update",
+  "diagnostic_referral:cancel",
+  "diagnostic_referral:read",
+  "diagnostic_result:acknowledge",
+  "whatsapp:enqueue",
+];
+
+function loadDotEnv() {
+  const path = resolve(ROOT, ".env");
+  if (!existsSync(path)) return;
+  for (const line of readFileSync(path, "utf8").split("\n")) {
+    const t = line.trim();
+    if (!t || t.startsWith("#")) continue;
+    const i = t.indexOf("=");
+    if (i < 0) continue;
+    const k = t.slice(0, i).trim();
+    const v = t.slice(i + 1).trim();
+    if (!(k in process.env)) process.env[k] = v;
+  }
+}
+
+function hashApiKey(raw) {
+  return createHash("sha256").update(raw).digest("hex");
+}
+
+async function main() {
+  loadDotEnv();
+  const dbUrl = process.env.DATABASE_URL;
+  if (!dbUrl) {
+    console.error("[bootstrap] DATABASE_URL not set");
+    process.exit(1);
+  }
+
+  const rawKey = process.env.HOPE_PARTNER_KEY || DEFAULT_PARTNER_KEY;
+  const callbackUrl =
+    process.env.INTEGRATION_HOPE_CALLBACK_URL || DEFAULT_CALLBACK;
+  const keyHash = hashApiKey(rawKey);
+  const keyPrefix = rawKey.slice(0, 14);
+
+  const client = new pg.Client({ connectionString: dbUrl });
+  await client.connect();
+
+  try {
+    const existing = await client.query(
+      `SELECT id FROM integration_partners WHERE code = 'HOPE' LIMIT 1`,
+    );
+
+    if (existing.rows.length === 0) {
+      await client.query(
+        `INSERT INTO integration_partners
+           (code, name, direction, key_hash, key_prefix, callback_url,
+            source_org_code, destination_org_code, permissions, is_active, created_by)
+         VALUES ('HOPE', 'Hope NeuroTrauma Hospital', 'inbound', $1, $2, $3,
+                 'HOPE', 'CARE', $4::jsonb, true, 'bootstrap-script')`,
+        [keyHash, keyPrefix, callbackUrl, JSON.stringify(HOPE_PERMISSIONS)],
+      );
+      console.log("[bootstrap] Created HOPE integration partner");
+    } else {
+      await client.query(
+        `UPDATE integration_partners
+            SET key_hash = $1, key_prefix = $2, callback_url = $3,
+                permissions = $4::jsonb, is_active = true, updated_at = now()
+          WHERE code = 'HOPE'`,
+        [keyHash, keyPrefix, callbackUrl, JSON.stringify(HOPE_PERMISSIONS)],
+      );
+      console.log("[bootstrap] Updated HOPE integration partner key + callback");
+    }
+
+    await client.query(
+      `INSERT INTO feature_flags (key, enabled, description)
+       VALUES ('ff_hope_care_referrals', true,
+               'HOPE → CARE diagnostic referral integration')
+       ON CONFLICT (key) DO UPDATE SET enabled = true`,
+    );
+    console.log("[bootstrap] Enabled ff_hope_care_referrals");
+
+    console.log("\n[bootstrap] Done. Verify:");
+    console.log("  Hope CARE_PARTNER_KEY matches intgk_… in hope.env");
+    console.log("  Hope CARE_CALLBACK_SECRET == Care INTEGRATION_HOPE_SIGNING_SECRET");
+    console.log(`  Callback URL: ${callbackUrl}`);
+  } finally {
+    await client.end();
+  }
+}
+
+main().catch((err) => {
+  console.error("[bootstrap] Failed:", err.message);
+  process.exit(1);
+});
-- 
2.43.0

